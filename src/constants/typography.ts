// Source of truth moved to src/theme/typography — import from there in new code.
// This file re-exports everything so existing imports continue to work.
export { typography } from "@/src/theme";
export type { TypographyToken } from "@/src/theme";
