import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  root: {
    flex: 1,
    backgroundColor: "#F5F6FB",
  },
  cartBannerWrap: {
    position: "absolute",
    left: 0,
    right: 0,
    zIndex: 50,
  },
});
